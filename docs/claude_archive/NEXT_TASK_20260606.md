# NEXT TASK

Completed in this session:
- One-click Stooq demo snapshot + end-to-end replay flow (2026-05-28)
  - New callbacks in gradio_dashboard.py:
    _cb_create_stooq_demo_snapshot, _cb_create_stooq_demo_snapshot_with_id,
    _cb_get_latest_demo_snapshot_id, _cb_create_and_replay_demo_snapshot
  - Tab 4 (Ingestion Pipeline): "Create Local Stooq Demo Snapshot" + "Create + Replay" buttons
  - Tab 5 (Snapshot Store): "Load Latest Demo Snapshot ID" helper
  - Tab 11 (CEO / Owner Brief): "Load Latest Demo Snapshot ID" helper
  - backend/tests/test_stooq_replay.py: 28 new tests (all passing)
  - Demo store: stooq_demo_snapshots.jsonl (paper_safe, FIXTURE_DATA, NO_EXECUTION)
- Ruff: All checks passed
- Pytest: 462 passed (was 434)
- verify_and_snapshot.py: eyay_clean_20260528T135613Z.zip

Recommended next tasks (in order of value):
1. Multi-day rolling diagnostics test:
   Create a second fixture multi-day CSV and write multi-snapshot rolling diagnostics tests
   to properly exercise the rolling window logic with 2+ snapshots.

2. Live fetch gate (optional, low risk):
   Add a "Fetch Live Stooq Data" button behind an explicit confirmation modal/checkbox
   (e.g. gr.Checkbox("I confirm live network is allowed")) — completely separate from fixture mode.
   IMPORTANT: Only implement if user explicitly requests live fetch.

3. Scheduled fixture snapshot (optional):
   Add a scheduled cron entry (e.g. daily at market close) to auto-create a Stooq fixture
   snapshot and persist it to stooq_demo_snapshots.jsonl — for review-only rolling diagnostics.

4. Source registry extension:
   When ready, separate multi-source-per-asset indexing in build_provider_source_bindings()
   so Stooq entries can be added to source_registry_v1.0.yaml without overwriting existing
   approved_* bindings. Currently blocked by:
   - test_unverified_registry_entries_are_simulation_only (asserts exact {BTCXAUK, XAUUSDK, XAGUSDK})
   - test_source_freshness_and_feature_diagnostics_flag_stale_sources (total_active_sources == total_sources)
