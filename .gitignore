# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/
*.egg

# Virtual environments
.venv/
venv/
env/
ENV/

# Pytest / coverage
.pytest_cache/
.pytest_tmp/
.coverage
htmlcov/
.snapshot_tmp/

# Clean backups (large zip files — don't commit)
clean_backups/

# Local snapshot data (paper snapshots — local only)
*.jsonl

# Environment files
.env
.env.*
*.env

# IDE
.vscode/
.idea/
*.swp
*.swo

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Docker volumes
docker/data/

# Node (future frontend)
node_modules/
.next/
dist/
.nuxt/

# Logs
*.log
logs/

# Internal tooling (Claude Code, Codex tmp)
.claude/
.codex_tmp/

# Alembic generated versions (keep only the base)
# backend/alembic/versions/

# pyc cache in root
*.pyc

# Signal chain runtime state — observational tracker rewrites this every tick
# (like paper_trading_state.json). Code recreates it as {"chains": {}} when
# absent, so it must not be tracked to avoid churn in diffs.
backend/data/signal_chain_state.json
